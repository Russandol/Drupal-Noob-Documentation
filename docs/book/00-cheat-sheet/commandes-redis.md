Les commandes de redis-cli

Tester si **Redis** fonctionne (doit renvoyer `PONG`): 
```shell
ping
```

Affiche des informations détaillées sur le serveur Redis (mémoire utilisée, nombre de clés, version, uptime…):
```shell
info
```

Afficher le moniteur:
```shell
monitor
```
