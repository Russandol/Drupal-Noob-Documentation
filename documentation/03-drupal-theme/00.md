# Plan

- présentation des thèmes
  - default
  - admin
- installer un theme d'administration : avec composer et Drush
  - gin
  - gin_toolbar
- installer un thème par défaut : avec l'administration de Drupal
  - bootstrap5
- créer son propre thème à partir d'un thème parent.
- organiser les assets
  - ajouter un compilateur à Docker
  - l'architecture des assets
  - surcharger bootstrap
- Surcharger un thème
  - activer le mode débug
  - respecter l'architecture



# Modules à activer

field : ajouter des champs aux types de contenu.
field_ui : gestion des champs dans les types de contenu.
path : ajouter des urls alias aux pages.
menu_ui : gérer les menus.

# THEME admin

composer require drupal/gin
drush theme:enable gin
composer require drupal/gin_toolbar
drush config:set system.theme default mon_theme -y
drush config:set system.theme admin mon_theme_admin -y
drush en gin_toolbar

dans l'admin, le définir comme theme pour l'administration. (vérifier la commande drush pour ça)